Set up or optimize Conan profile configuration for ESP32 cross-compilation. Use mcp-prompts to get Conan best practices:

1. Check current Conan profiles in `conan-profiles/`
2. Use mcp-prompts `/conan` command for cross-compilation setup guidance
3. Generate or update profiles for ESP32, ESP32-S3, Arduino targets
4. Verify package dependencies and build requirements

Reference: `.claude/mcp.json` has unified-dev-tools MCP server configured for Conan operations.

